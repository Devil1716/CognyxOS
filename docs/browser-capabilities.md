# Browser capabilities

The universal browser surface includes open, navigate, back, forward, reload, read, click, type, select, download, upload, screenshot, tabs, windows, and permitted cookies. It intentionally contains no Playwright types. A future Playwright or native-browser provider can implement these definitions without changing the Agent Kernel.
