# Capability registry

`CapabilityRegistry` registers, unregisters, looks up, lists, searches, versions, and discovers providers. Compatible versions share a major version and may satisfy an equal-or-lower requested minor version. Provider discovery answers which provider and runtime can supply a capability.
